CREATE TABLE usuario ( id bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, nome VARCHAR(255) NOT NULL, email VARCHAR(255) NOT NULL, username VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, role VARCHAR(100), PRIMARY KEY (id));